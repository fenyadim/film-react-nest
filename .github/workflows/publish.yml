name: Create and publish a Docker image

on: push

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-publish-latest:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - dockerfile: backend/Dockerfile
            image: ghcr.io/fenyadim/film-nest-backend
            path: backend
          - dockerfile: frontend/Dockerfile
            image: ghcr.io/fenyadim/film-nest-frontend
            path: frontend
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4 # Checking out the repo

      - name: login to docker hub
        run: echo "${{ secrets.GH_TOKEN }}" | docker login --username fenyadim --password ${{ secrets.GH_TOKEN }} ghcr.io

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@9ec57ed1fcdbf14dcef7dfbe97b2010124a938b7
        with:
          images: ${{ matrix.image }}

      - name: Build and push Docker image
        uses: docker/build-push-action@f2a1d5e99d037542a71f64918e516c093c6f3fc4
        with:
          context: ${{ matrix.path }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: latest
          labels: ${{ steps.meta.outputs.labels }}
